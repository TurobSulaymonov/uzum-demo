/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
   images: {
    domains: ['utfs.io'], // 🔥 Shu qatorni qo‘sh
  },
  // qo'shimcha config bo'lsa shu yerga yoz
};

module.exports = nextConfig;